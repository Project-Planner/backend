package web

import "time"

const (
	authorizedStr = "authorizedStr"
	tokenIDStr    = "token_id"
	userIDStr     = "user_id"
	expiryStr     = "expiry"
	authStr       = "auth"
	jwtDuration   = time.Hour * 365 * 24
)
