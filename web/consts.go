package web

import "time"

const (
	authorizedStr = "authorizedStr"
	tokenIDStr    = "token_id"
	userIDStr     = "user_id"
	calendarIDStr = "calendar_id"
	itemIDStr     = "item_id"
	expiryStr     = "expiry"
	authStr       = "auth"
	jwtDuration   = time.Hour * 365 * 24
)
